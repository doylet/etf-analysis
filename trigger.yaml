# Trigger configuration for Cloud Build
# Import this trigger via: gcloud builds triggers import --source=trigger.yaml

name: etf-analysis-deploy
description: Auto-deploy ETF Analysis Dashboard on push to master
filename: cloudbuild.yaml

github:
  owner: doylet
  name: etf-analysis
  push:
    branch: ^master$
